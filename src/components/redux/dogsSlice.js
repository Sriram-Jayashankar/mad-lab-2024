import { createSlice } from "@reduxjs/toolkit";

const initialState={
    founddog:[],
    lostdof:[]
}

const dogSlice=createSlice({
    name:"lostAndFoundDogs",
    initialState,
    reducers:{
        s
    }
})